nome = "\tmaicon cutrim\n"
print(nome.title())
print(nome.title().lstrip()) # remove os espaços do lado esquerdo
print(nome.title().rstrip()) # remove os espaços do lado direito
print(nome.title().strip()) # remove os espaçõs de ambos os lados
# atribui um nome a uma variável com tabulação e quebra de linha e faz a remoção desses espaços
